name: flutter_weather_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  http: ^1.0.0
  geolocator: ^10.0.0 
  geocoding: ^2.1.0
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.6
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # La ligne suivante doit être alignée avec la ligne "assets:"
  uses-material-design: true

  # --- DÉBUT DE LA CORRECTION ---
  # Assurez-vous que "assets:" a deux espaces devant.
  assets:
    # Assurez-vous que cette ligne a quatre espaces devant et se termine par un /
    - assets/images/
  # --- FIN DE LA CORRECTION ---